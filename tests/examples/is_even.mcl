@name is_even

var test
test = 2
chat system = ["# OUT #", test]

system.log(@s, ["i wish it works, dude"], True)

func check(x)
    if x % 2 == 0
        system.log(@s, ["True"])
    system.log(@s, ["False"])