@name is_even

chat system = ["# OUT #"]

system.log(@a, ["i wish it works, dude"])

func check(x)
    if x % 2 == 0
        system.log(@s, ["True"])
        return True
    system.log(@s, ["False"])
    return False