@name ptest

@define expression particle_generator
@define block mod_block
@define block spawn_block

image lmao = "texture.png"

input activate(num) -> @a
    mod at @s
        spawn minecraft:armor_stand
            self.pargen = true

rule pgen(pargen) -> minecraft:armor_stand
    mod at @s
        lmao.show()